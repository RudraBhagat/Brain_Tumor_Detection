# Core Web Framework
Flask==3.0.3
Flask-Cors==4.0.1

# Deep Learning & Machine Learning
# Stable versions for Python 3.11 runtime
tensorflow==2.20.0
numpy==1.26.4

# Image Processing
# Use headless build for cloud Linux containers (avoids libGL.so.1 errors)
opencv-python-headless==4.9.0.80

# Production WSGI server
gunicorn==22.0.0
